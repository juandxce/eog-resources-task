

const initialState = ['red', 'blue', 'brown', 'orange', 'gray', 'green', 'purple'];

export const colorsReducer = (state: any = initialState, action: any) => {
  //TODO: add functionality to be able to change colors
  return state;
}
